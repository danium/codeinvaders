# CLI lifecycle fixtures

These fixtures model preconfigured shared agent settings for platform-neutral
composition tests. They intentionally contain harmless labels only. Native
payloads, credentials, prompts, paths, and command arguments do not belong in
CLI fixtures.
