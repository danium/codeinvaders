# Core fixtures

These fixtures are sanitized, opaque-label scenarios for journal recovery, reducer terminality, gaps, and path ownership. They contain no native transcripts, paths, commands, or adapter payloads.
